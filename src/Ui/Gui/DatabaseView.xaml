<UserControl
    x:Class="Media.Ui.Gui.DatabaseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Media.Ui.Gui"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ScrollViewer
            Grid.Column="0"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Expander
                    Header="Last played"
                    IsExpanded="True">
                    <StackPanel>
                        <Button Style="{StaticResource LinkButton}" Content="Today" />
                        <Button Style="{StaticResource LinkButton}" Content="Last 3 days" />
                        <Button Style="{StaticResource LinkButton}" Content="This week" />
                        <Button Style="{StaticResource LinkButton}" Content="This Month" />
                        <Button Style="{StaticResource LinkButton}" Content="This year" />
                        <Button Style="{StaticResource LinkButton}" Content="All time" />
                    </StackPanel>
                </Expander>
            </StackPanel>
        </ScrollViewer>
        <DataGrid
            x:Name="DatabaseGrid"
            Grid.Column="1"
            SelectionMode="Single"
            SelectionUnit="FullRow"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserReorderColumns="True"
            CanUserResizeColumns="True"
            CanUserResizeRows="False"
            CanUserSortColumns="True"
            AutoGenerateColumns="True" />
    </Grid>
</UserControl>
